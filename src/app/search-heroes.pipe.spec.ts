import { SearchHeroesPipe } from './search-heroes.pipe';

describe('SearchHeroesPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchHeroesPipe();
    expect(pipe).toBeTruthy();
  });
});
